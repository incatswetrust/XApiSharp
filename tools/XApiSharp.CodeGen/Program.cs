// Dev-time CLI: wraps the chosen OpenAPI generator (spec section 7) and the
// endpoint-manifest / coverage report tooling (spec section 5). Not shipped
// as a NuGet dependency to SDK consumers.
Console.WriteLine("XApiSharp.CodeGen: not implemented yet (stage E0/E1).");
return 0;
