# Samples

Compilable sample applications (spec section 20), to be added as the corresponding SDK surface
lands:

- Console read
- ASP.NET Core OAuth 2.0 / PKCE
- User post with explicit write trigger
- Pagination
- Media upload
- Filtered stream
- Webhook receiver

None exist yet — stage E2 introduces the first (Console read) alongside the initial vertical
slice.
